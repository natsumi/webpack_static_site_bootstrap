import '../stylesheets/app';

console.log('Welcome to your webpack static site generator');
console.log(process.env.NODE_ENV);

// es6 support
const x = [1, 2, 3, 4];
x.forEach(num => {
  console.log(num);
});
